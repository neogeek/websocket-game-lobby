language: node_js
node_js:
    - '12'
branches:
    only:
        - master
after_success:
    - node --version
    - npm --version
    - npm test
    - npm run coverage
    - bash <(curl -s https://codecov.io/bash)
sudo: false
