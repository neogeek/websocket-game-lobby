game:
  gameId: string
  gameCode: string
  started: boolean
  players: [player]
  spectators: [spectator]
  turns: [turn]
  custom: object

player:
  playerId: string
  gameId: string
  name: string
  isAdmin: boolean
  custom: object

spectator:
  spectatorId: string
  gameId: string
  name: string
  custom: object

turn:
  turnId: string
  gameId: string
  index: number
  custom: object
