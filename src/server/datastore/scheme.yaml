game:
  gameId: string
  gameCode: string
  started: boolean
  players: [player]
  spectators: [spectator]
  turns: [turn]
  custom: object

player:
  playerId: string
  name: string
  custom: object

spectator:
  spectatorId: string
  name: string
  custom: object

turn:
  turnId: string
  index: number
  custom: object
