game:
  gameId: string
  gameCode: string
  started: boolean
  players: [player]
  spectators: [spectator]
  turns: [turn]

player:
  playerId: string
  name: string

spectator:
  spectatorId: string
  name: string

turn:
  turnId: string
